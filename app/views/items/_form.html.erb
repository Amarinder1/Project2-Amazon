<%= form_for @item do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :pic_url %>
  <%= f.text_field :pic_url %>

  <%= f.label :price %>
  <%= f.text_field :price %>

  <%= f.submit %>

<% end %>
