<h1>All the items</h1>
<ul>
  <% @items.each do |item| %>
    <li>
      <%= link_to item.name, item_path(item) %>
      <%= item.price %>
      <%= image_tag item.pic_url %>
    </li>
  <% end %>
</ul>
