<div class="item">
  <ul>
  <%= image_tag @item.pic_url, :class => "style_image" %>
  <br><h2><%= @item.name %></h2>
  <h2>Price $<%= @item.price %></h2>
  <button class="button"><%= link_to 'Add to Cart', add_to_cart_item_path(@item), method: :post, class: "button"%></button>
  <ul>
</div>
